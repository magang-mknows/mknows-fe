export type TusePopupDeleteDiscussion = {
  setPopupDeleteStatus: (val: boolean) => void;
  getPopupDeleteStatus: boolean;
};

export type TusePopupEditDiscussion = {
  setPopupEditStatus: (val: boolean) => void;
  getPopupEditStatus: boolean;
};
