import { FC, ReactElement } from 'react';

export const FeatureSection: FC = (): ReactElement => {
  return (
    <section className="flex z-10 w-full h-[140px] justify-between bg-primary-500">
      Test
    </section>
  );
};
